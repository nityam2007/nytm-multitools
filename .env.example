# NYTM Tools - Environment Variables
# Copy this file to .env.local and update the values

# ===========================================
# AUTHENTICATION (Required for Admin Panel)
# ===========================================

# Admin password for accessing the admin dashboard
# Use a strong, unique password
AUTH_PASSWORD=your_secure_admin_password_here

# Secret key for JWT token signing
# Generate with: openssl rand -base64 32
AUTH_SECRET=your_jwt_secret_key_here_min_32_chars

# ===========================================
# DATABASE (Optional - defaults work fine)
# ===========================================

# SQLite database path (relative to project root)
# Default: ./data/archive.sqlite
# DATABASE_PATH=./data/archive.sqlite

# ===========================================
# APPLICATION (Optional)
# ===========================================

# Application URL (used for metadata)
# Default: http://localhost:3000
NEXT_PUBLIC_APP_URL=https://nytm.in

# Application name
NEXT_PUBLIC_APP_NAME=NYTM Tools

# ===========================================
# DEPLOYMENT NOTES
# ===========================================

# For Vercel:
# - Add these variables in Project Settings > Environment Variables
# - The data directory will be ephemeral; consider external DB for production

# For VPS (Hostinger, DigitalOcean, etc.):
# - Copy .env.example to .env.local
# - Ensure /data directory is writable
# - Use PM2 or systemd for process management

# For Netlify:
# - Add variables in Site Settings > Environment Variables
# - Note: Netlify Functions have different constraints than Vercel
